
const getters = {
  get_reasons:            (state) => state.reasons,
  get_total:              (state) => state.total,
  get_status_event:       (state) => state.status_event,
  get_total_status_event: (state) => state.total_status_event,

}

export default getters
